name: Markdown Lint
on:
  pull_request: {}
permissions:
  statuses: write
  checks: write
  contents: read
  pull-requests: read
jobs:
  markdown-lint:
    uses: tfelab/actions/.github/workflows/markdown-lint.yml@main
